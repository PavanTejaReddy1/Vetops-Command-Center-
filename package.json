{
  "name": "vetops-command-center",
  "version": "1.0.0",
  "private": true,
  "description": "VetOps Command Center monorepo — frontend (React/Vite) + backend (Express/MongoDB)",
  "scripts": {
    "install:all": "npm ci --prefix backend && npm ci --prefix frontend",
    "build": "npm ci --prefix backend && npm ci --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
