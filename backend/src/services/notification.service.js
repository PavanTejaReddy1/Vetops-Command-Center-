/**
 * Notifications service — PHASE 2.
 * Business logic and MongoDB queries for Notifications will live here.
 * Controllers should stay thin and delegate to this layer.
 */
export const notificationService = {
  async list() {
    throw new Error('notificationService.list() not implemented — Phase 1 is structure only.');
  },
  async getById(id) {
    throw new Error('notificationService.getById() not implemented — Phase 1 is structure only.');
  },
  async create(data) {
    throw new Error('notificationService.create() not implemented — Phase 1 is structure only.');
  },
  async update(id, data) {
    throw new Error('notificationService.update() not implemented — Phase 1 is structure only.');
  },
  async remove(id) {
    throw new Error('notificationService.remove() not implemented — Phase 1 is structure only.');
  },
};
