/**
 * Forecast & Capacity service — PHASE 2.
 * Business logic and MongoDB queries for Forecast & Capacity will live here.
 * Controllers should stay thin and delegate to this layer.
 */
export const forecastService = {
  async list() {
    throw new Error('forecastService.list() not implemented — Phase 1 is structure only.');
  },
  async getById(id) {
    throw new Error('forecastService.getById() not implemented — Phase 1 is structure only.');
  },
  async create(data) {
    throw new Error('forecastService.create() not implemented — Phase 1 is structure only.');
  },
  async update(id, data) {
    throw new Error('forecastService.update() not implemented — Phase 1 is structure only.');
  },
  async remove(id) {
    throw new Error('forecastService.remove() not implemented — Phase 1 is structure only.');
  },
};
