/**
 * Workflow Queue / Appointments service — PHASE 2.
 * Business logic and MongoDB queries for Workflow Queue / Appointments will live here.
 * Controllers should stay thin and delegate to this layer.
 */
export const appointmentService = {
  async list() {
    throw new Error('appointmentService.list() not implemented — Phase 1 is structure only.');
  },
  async getById(id) {
    throw new Error('appointmentService.getById() not implemented — Phase 1 is structure only.');
  },
  async create(data) {
    throw new Error('appointmentService.create() not implemented — Phase 1 is structure only.');
  },
  async update(id, data) {
    throw new Error('appointmentService.update() not implemented — Phase 1 is structure only.');
  },
  async remove(id) {
    throw new Error('appointmentService.remove() not implemented — Phase 1 is structure only.');
  },
};
