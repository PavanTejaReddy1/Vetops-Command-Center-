/**
 * Veterinarians service — PHASE 2.
 * Business logic and MongoDB queries for Veterinarians will live here.
 * Controllers should stay thin and delegate to this layer.
 */
export const veterinarianService = {
  async list() {
    throw new Error('veterinarianService.list() not implemented — Phase 1 is structure only.');
  },
  async getById(id) {
    throw new Error('veterinarianService.getById() not implemented — Phase 1 is structure only.');
  },
  async create(data) {
    throw new Error('veterinarianService.create() not implemented — Phase 1 is structure only.');
  },
  async update(id, data) {
    throw new Error('veterinarianService.update() not implemented — Phase 1 is structure only.');
  },
  async remove(id) {
    throw new Error('veterinarianService.remove() not implemented — Phase 1 is structure only.');
  },
};
