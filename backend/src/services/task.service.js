/**
 * Task Assignment service — PHASE 2.
 * Business logic and MongoDB queries for Task Assignment will live here.
 * Controllers should stay thin and delegate to this layer.
 */
export const taskService = {
  async list() {
    throw new Error('taskService.list() not implemented — Phase 1 is structure only.');
  },
  async getById(id) {
    throw new Error('taskService.getById() not implemented — Phase 1 is structure only.');
  },
  async create(data) {
    throw new Error('taskService.create() not implemented — Phase 1 is structure only.');
  },
  async update(id, data) {
    throw new Error('taskService.update() not implemented — Phase 1 is structure only.');
  },
  async remove(id) {
    throw new Error('taskService.remove() not implemented — Phase 1 is structure only.');
  },
};
