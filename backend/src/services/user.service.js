/**
 * Users service — PHASE 2.
 * Business logic and MongoDB queries for Users will live here.
 * Controllers should stay thin and delegate to this layer.
 */
export const userService = {
  async list() {
    throw new Error('userService.list() not implemented — Phase 1 is structure only.');
  },
  async getById(id) {
    throw new Error('userService.getById() not implemented — Phase 1 is structure only.');
  },
  async create(data) {
    throw new Error('userService.create() not implemented — Phase 1 is structure only.');
  },
  async update(id, data) {
    throw new Error('userService.update() not implemented — Phase 1 is structure only.');
  },
  async remove(id) {
    throw new Error('userService.remove() not implemented — Phase 1 is structure only.');
  },
};
