/**
 * Audit Logs service — PHASE 2.
 * Business logic and MongoDB queries for Audit Logs will live here.
 * Controllers should stay thin and delegate to this layer.
 */
export const auditLogService = {
  async list() {
    throw new Error('auditLogService.list() not implemented — Phase 1 is structure only.');
  },
  async getById(id) {
    throw new Error('auditLogService.getById() not implemented — Phase 1 is structure only.');
  },
  async create(data) {
    throw new Error('auditLogService.create() not implemented — Phase 1 is structure only.');
  },
  async update(id, data) {
    throw new Error('auditLogService.update() not implemented — Phase 1 is structure only.');
  },
  async remove(id) {
    throw new Error('auditLogService.remove() not implemented — Phase 1 is structure only.');
  },
};
