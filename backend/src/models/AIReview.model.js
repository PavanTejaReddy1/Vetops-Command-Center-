/**
 * AIReview model — PHASE 2 (Mongoose schema not implemented yet).
 *
 * Structure placeholder so the shape of the future schema is discoverable
 * from the codebase. Fields below are illustrative only, based on the
 * dummy data used by the frontend (src/data/aiReview.js).
 *
 * Example (for Phase 2 reference):
 *
 * import mongoose from 'mongoose';
 *
 * const aiReviewSchema = new mongoose.Schema(
 *   {
 *     // ...fields specific to AI Review
 *   },
 *   { timestamps: true }
 * );
 *
 * export const AIReview = mongoose.model('AIReview', aiReviewSchema);
 */
export const AIReviewModel = null; // placeholder export until Phase 2
