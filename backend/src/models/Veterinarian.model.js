/**
 * Veterinarian model — PHASE 2 (Mongoose schema not implemented yet).
 *
 * Structure placeholder so the shape of the future schema is discoverable
 * from the codebase. Fields below are illustrative only, based on the
 * dummy data used by the frontend (src/data/veterinarian.js).
 *
 * Example (for Phase 2 reference):
 *
 * import mongoose from 'mongoose';
 *
 * const veterinarianSchema = new mongoose.Schema(
 *   {
 *     // ...fields specific to Veterinarians
 *   },
 *   { timestamps: true }
 * );
 *
 * export const Veterinarian = mongoose.model('Veterinarian', veterinarianSchema);
 */
export const VeterinarianModel = null; // placeholder export until Phase 2
