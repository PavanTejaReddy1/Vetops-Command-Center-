# Server
PORT=5000
NODE_ENV=development

# Database (Phase 2)
MONGODB_URI=mongodb://localhost:27017/vetops

# Auth (Phase 2)
JWT_SECRET=replace-with-a-real-secret
JWT_EXPIRES_IN=7d

# AI Integration (Phase 2 — Groq)
GROQ_API_KEY=your-groq-api-key-here
